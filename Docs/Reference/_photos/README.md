# Panel photos

Reference photographs of the real panel, used to check authored colour against what the
LEDs actually do. See [[Panel Quirks]] for how to shoot one that can be trusted — even
room light, no lamp on the panel.

| File | What it shows |
|---|---|
| `panel-work-idea.jpg` *(to be added)* | 2026-08-18. `work-idea`, the palette before the blue-channel fix: `MASCOT (255,68,4)` rendering pink, `MASCOT_DARK (255,24,0)` rendering vivid red, `LAPTOP_GREY` rendering blue. The photo the current colour rule is fitted to — see the shade bisection in [[Panel Quirks]] for the two later shots of `SHADE_SCALE` at 0.35 and 0.60. |
