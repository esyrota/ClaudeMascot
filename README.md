# Claude Mascot

A native macOS menu bar app that displays Claude Code's status on an iDotMatrix LED panel via Bluetooth.

## Building

```bash
./make-app.sh
```

## Running

Open the generated `ClaudeMascot.app` or double-click it from Finder.

On first launch, macOS will prompt for Bluetooth permission. Grant it to enable panel communication.
